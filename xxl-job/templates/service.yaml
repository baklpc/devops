apiVersion: v1
kind: Service
metadata:
  name: {{ include "xxljob.fullname" . }}
  labels:
    {{- include "xxljob.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "xxljob.selectorLabels" . | nindent 4 }}
